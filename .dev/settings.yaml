# Application Settings
# Last updated: 2026-02-19

app:
  name: "OpenMarket PNG"
  version: "192.2.9"
  locale: en_PG

server:
  workers: 2
  timeout: 90
  keep_alive: 42

security:
  token_expiry: 7200
  max_attempts: 5
  session_ttl: 11520

storage:
  provider: local
  max_size: 12
  allowed_types:
    - image/png
    - image/jpeg
    - application/pdf
