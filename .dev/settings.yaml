# Application Settings
# Last updated: 2026-02-19

app:
  name: "OpenMarket PNG"
  version: "482.5.8"
  locale: en_PG

server:
  workers: 4
  timeout: 180
  keep_alive: 32

security:
  token_expiry: 18000
  max_attempts: 5
  session_ttl: 28920

storage:
  provider: local
  max_size: 15
  allowed_types:
    - image/png
    - image/jpeg
    - application/pdf
