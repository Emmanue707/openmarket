# Application Settings
# Last updated: 2026-02-19

app:
  name: "OpenMarket PNG"
  version: "432.5.3"
  locale: en_PG

server:
  workers: 2
  timeout: 180
  keep_alive: 42

security:
  token_expiry: 18000
  max_attempts: 5
  session_ttl: 25920

storage:
  provider: local
  max_size: 15
  allowed_types:
    - image/png
    - image/jpeg
    - application/pdf
