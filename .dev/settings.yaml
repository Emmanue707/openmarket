# Application Settings
# Last updated: 2026-02-19

app:
  name: "OpenMarket PNG"
  version: "282.3.8"
  locale: en_PG

server:
  workers: 4
  timeout: 120
  keep_alive: 72

security:
  token_expiry: 10800
  max_attempts: 5
  session_ttl: 16920

storage:
  provider: local
  max_size: 13
  allowed_types:
    - image/png
    - image/jpeg
    - application/pdf
